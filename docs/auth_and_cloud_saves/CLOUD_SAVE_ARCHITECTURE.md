# Cloud save architecture

The cloud layer stores snapshots of the full save payload keyed by the authenticated user id.
Local saves remain the primary immediate source and are still available offline.
